<%- include('../partials/head') %>

    <body class="bg-mesh">
        <div class="container-fluid p-0">
            <div class="row g-0 justify-content-center align-items-center vh-100">
                <div class="col-md-5 fade-in">
                    <div class="premium-card p-4 p-lg-5">
                        <div class="text-center mb-4">
                            <div class="d-inline-flex align-items-center justify-content-center bg-danger bg-opacity-10 rounded-circle mb-3"
                                style="width: 64px; height: 64px;">
                                <i class="fas fa-bolt text-danger fs-3"></i>
                            </div>
                            <h4 class="fw-bold text-main mb-1">Verify Freecharge</h4>
                            <p class="text-muted small">Connect your Freecharge merchant account</p>
                        </div>

                        <form method="POST" action="/auth/freecharge">
                            <div class="mb-3">
                                <label for="MID"
                                    class="form-label extra-small fw-bold text-dim uppercase tracking-widest">app_fc
                                    Cookie</label>
                                <input type="text" name="MID" id="MID" placeholder="Enter Cookie"
                                    value="<%= merchant ? merchant.freecharge_app_fc : '' %>"
                                    class="form-control form-control-premium" required>
                            </div>
                            <div class="mb-3">
                                <label for="Number"
                                    class="form-label extra-small fw-bold text-dim uppercase tracking-widest">Mobile
                                    Number</label>
                                <input type="number" name="Number" id="Number" placeholder="Enter Number"
                                    value="<%= merchant ? merchant.freecharge_number : '' %>"
                                    class="form-control form-control-premium" required minlength="10" maxlength="10">
                            </div>
                            <div class="mb-4">
                                <label for="UPI"
                                    class="form-label extra-small fw-bold text-dim uppercase tracking-widest">Freecharge
                                    UPI ID</label>
                                <input type="text" name="UPI" id="UPI" placeholder="e.g. merchant@freecharge"
                                    value="<%= merchant ? merchant.freecharge_upi_id : '' %>"
                                    class="form-control form-control-premium" required>
                            </div>

                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-premium py-3">Verify & Connect</button>
                                <a href="/auth/connect_merchant"
                                    class="btn btn-link text-muted text-decoration-none small">Go Back</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <%- include('../partials/footer') %>
    </body>

    </html>